# PostgreSQL credentials for docker compose
# Copy this file to .env and adjust if needed.
#
# Usage: cp .env.example .env

DB_HOST=localhost
DB_PORT=5432
DB_NAME=binance_quotes
DB_USER=postgres
DB_PASSWORD=postgres
